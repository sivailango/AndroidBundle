<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aurum">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TabActivity" />
        <activity android:name=".activities.FabActivity" />
        <activity android:name=".activities.ImageActivity" />
        <activity android:name=".activities.ResponsiveActivity" />
        <activity android:name=".activities.HorizontalScrollView" />
        <activity android:name=".activities.DropShadowActivity" />
        <activity android:name=".activities.AnimationActivity" />
        <activity android:name=".activities.DialogActivity" />
        <activity android:name=".activities.MapsActivity" />
        <activity android:name=".activities.AndroidPayActivity" />
        <activity android:name=".activities.DropInActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <activity
            android:name=".activities.SplashActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />

        <activity android:name=".activities.NfcActivity" />

        <activity android:name=".activities.FingerprintActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>